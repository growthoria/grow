export async function onRequestGet(context){const kv=context.env.GROWTHORIA_KV;if(!kv)return new Response(JSON.stringify({}),{headers:{'content-type':'application/json'}});const saved=await kv.get('site-content');return new Response(saved||'{}',{headers:{'content-type':'application/json','cache-control':'no-store'}})}
export async function onRequestPost(context){const password=context.request.headers.get('x-admin-password');if(!context.env.ADMIN_PASSWORD||password!==context.env.ADMIN_PASSWORD)return new Response('Unauthorized',{status:401});const kv=context.env.GROWTHORIA_KV;if(!kv)return new Response('Missing GROWTHORIA_KV binding',{status:500});const content=await context.request.json();await kv.put('site-content',JSON.stringify(content));return new Response(JSON.stringify({ok:true}),{headers:{'content-type':'application/json','cache-control':'no-store'}})}
